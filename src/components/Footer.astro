---
import { getI18N } from "@/i18n";
import SocialLink from "./SocialLink.astro";

const { currentLocale } = Astro;
const i18n = getI18N({ currentLocale });

const currentYear = new Date().getFullYear();
---

<footer
  class="opacity-80 m-4 min-[375px]:px-4 sm:pl-0 mt-16 w-full mx-auto container lg:max-w-4xl md:max-w-2xl mb-10 flex justify-center"
>
  <div
    class="w-full max-w-screen-xl mx-auto flex items-center justify-center flex-col gap-1 md:justify-between py-4"
  >
    <span class="text-sm sm:text-center text-zinc-800/90 dark:text-zinc-200/90"
      >© {currentYear}
      {i18n.FOOTER.TEXT}
      <span aria-label={i18n.FOOTER.FACE_EMOJI}>💻</span>
     
      <a
        href="https://www.linkedin.com/in/ignacioperrote/"
        class="hover:underline">{i18n.FOOTER.TEXT2}</a
      >
      <span aria-label={i18n.FOOTER.ROCK_EMOJI}>🤘</span>
    </span>
    <span></span>
    <nav class="flex gap-4">
      <SocialLink icon="LinkedIn" />
      <SocialLink icon="GitHub" />
      <SocialLink icon="Mail" />
    </nav>
  </div>
</footer>
